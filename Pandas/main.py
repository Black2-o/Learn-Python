# Import the pandas library and give it the alias 'pd' for easier reference
import pandas as pd 

# Pandas Version: This checks the version of the pandas library installed in your environment
# It's useful to ensure compatibility with certain features or functions
print(pd.__version__)
